import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'luat.nguyen.dev',
  appName: 'my-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
